name: pr_tools
description: CLI for Azure DevOps pull requests and test cards.
version: 4.0.4
publish_to: none

environment:
  sdk: ^3.13.1

# Add regular dependencies here.
dependencies:
  better_effect: ^0.3.0
  dio: ^5.11.0
  genkit: ^0.15.1
  genkit_openai: ^0.3.7
  result_dart: ^2.2.0
  terminice: ^1.3.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  prt: prt
